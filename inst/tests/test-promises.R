context("Promises")

test_that("can create promise explicitly", {
  stop("test not written")
})

test_that("can create promise implicitly", {
  stop("test not written")
})

test_that("can get arg and environment of promise", {
  stop("test not written")
})

test_that("Can get forced, literal, etc of promise", {
  stop("test not written")
})
